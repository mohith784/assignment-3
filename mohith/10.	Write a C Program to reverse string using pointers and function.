#include <stdio.h>
void main()
{
	char *s;
	int len,i;
	printf("mohith chowdary");
	printf("\nENTER A STRING: ");
	gets(s);
	len=strlen(s);
	printf("\nTHE REVERSE OF THE STRING IS:");
	for(i=len;i>=0;i--)
		printf("%c",*(s+i));
}
